# Blind Agent Run

This run asks independent writer subagents to apply `blog-knowledge-extraction` to the synthetic raw samples.

Subagents were instructed not to read `raw-evals/expected.tsv`.

Outputs are stored in `outputs/`.
